---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getLocalizedPaths } from "../../utils/localization.ts";
import { t, defaultLocale, type Locale } from "../../i18n.ts";
import DeveloperHero from "../../components/DeveloperHero.astro";
import DeveloperPainPoints from "../../components/DeveloperPainPoints.astro";
import DeveloperGains from "../../components/DeveloperGains.astro";

export const getStaticPaths = getLocalizedPaths;

const lang = Astro.params.lang ?? defaultLocale;
const locale = lang as Locale;
---

<BaseLayout title={`TFST – ${t(locale, "forDevs.title")}`} lang={lang}>
    <DeveloperHero locale={locale} />
    <DeveloperPainPoints locale={locale} />
    <DeveloperGains locale={locale} />
</BaseLayout>
