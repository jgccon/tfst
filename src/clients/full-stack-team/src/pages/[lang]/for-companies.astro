---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getLocalizedPaths } from "../../utils/localization.ts";
import { t, defaultLocale, type Locale } from "../../i18n.ts";
import CompaniesHero from "../../components/CompaniesHero.astro";
import CompaniesPainPoints from "../../components/CompaniesPainPoints.astro";

export const getStaticPaths = getLocalizedPaths;

const lang = Astro.params.lang ?? defaultLocale;
const locale = lang as Locale;
---

<BaseLayout title={`TFST – ${t(locale, "forCompanies.title")}`} lang={lang}>
    <CompaniesHero locale={locale} />
    <CompaniesPainPoints locale={locale} />
</BaseLayout>
