---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getLocalizedPaths } from "../../utils/localization.ts";
import { t, defaultLocale, type Locale } from "../../i18n.ts";
import ProfessionalHero from "../../components/ProfessionalHero.astro";
import ProfessionalPainPoints from "../../components/ProfessionalPainPoints.astro";
import ProfessionalGains from "../../components/ProfessionalGains.astro";

export const getStaticPaths = getLocalizedPaths;

const lang = Astro.params.lang ?? defaultLocale;
const locale = lang as Locale;
---

<BaseLayout title={`TFST – ${t(locale, "forDevs.title")}`} lang={lang}>
    <ProfessionalHero locale={locale} />
    <ProfessionalPainPoints locale={locale} />
    <ProfessionalGains locale={locale} />
</BaseLayout>
