---
// src/components/CompaniesPainPoints.astro
import { t } from "../i18n";
const { locale } = Astro.props;
---

<section class="max-w-6xl mx-auto px-6 py-20">
  <header class="text-center mb-16">
    <h2 class="text-3xl font-bold mb-4">
      {t(locale, "forCompanies.painTitle")}
    </h2>
    <p class="text-gray-700 text-lg">
      {t(locale, "forCompanies.painSubtitle")}
    </p>
  </header>

  <div class="grid md:grid-cols-2 gap-8">
    <div class="pain-point-card">
      <h3 class="text-xl font-semibold mb-2">
        🎯 {t(locale, "forCompanies.painPoints.hiringTitle")}
      </h3>
      <p class="text-gray-700">
        {t(locale, "forCompanies.painPoints.hiring")}
      </p>
    </div>

    <div class="pain-point-card">
      <h3 class="text-xl font-semibold mb-2">
        💸 {t(locale, "forCompanies.painPoints.costTitle")}
      </h3>
      <p class="text-gray-700">
        {t(locale, "forCompanies.painPoints.cost")}
      </p>
    </div>

    <div class="pain-point-card">
      <h3 class="text-xl font-semibold mb-2">
        🕵️‍♂️ {t(locale, "forCompanies.painPoints.riskTitle")}
      </h3>
      <p class="text-gray-700">
        {t(locale, "forCompanies.painPoints.risk")}
      </p>
    </div>

    <div class="pain-point-card">
      <h3 class="text-xl font-semibold mb-2">
        🧱 {t(locale, "forCompanies.painPoints.fragileTitle")}
      </h3>
      <p class="text-gray-700">
        {t(locale, "forCompanies.painPoints.fragile")}
      </p>
    </div>
  </div>
</section>
