---
// DeveloperPitch.astro
import FeatureBox from './FeatureBox.astro';
import { t } from '../i18n.ts';

const { locale } = Astro.props;
---

<section class="max-w-6xl mx-auto px-4 py-20">
  <header class="text-center mb-16">
    <h1 class="text-4xl font-bold mb-4">{t(locale, "forDevs.heroTitle")}</h1>
    <p class="text-lg text-gray-700">{t(locale, "forDevs.heroSubtitle")}</p>
  </header>

  <div class="grid md:grid-cols-2 gap-12 mb-20">
    <FeatureBox
      icon="🧱"
      title={t(locale, "forDevs.modularTitle")}
      desc={t(locale, "forDevs.modularDesc")}
    />
    <FeatureBox
      icon="🔐"
      title={t(locale, "forDevs.authTitle")}
      desc={t(locale, "forDevs.authDesc")}
    />
    <FeatureBox
      icon="⚡"
      title={t(locale, "forDevs.devopsTitle")}
      desc={t(locale, "forDevs.devopsDesc")}
    />
    <FeatureBox
      icon="🤝"
      title={t(locale, "forDevs.openTitle")}
      desc={t(locale, "forDevs.openDesc")}
    />
  </div>

  <section class="text-center">
    <h2 class="text-2xl font-semibold mb-4">{t(locale, "forDevs.joinTitle")}</h2>
    <p class="text-gray-700 mb-6">{t(locale, "forDevs.joinDesc")}</p>
    <a
      href="https://github.com/jgccon/tfst"
      target="_blank"
      class="inline-block bg-black text-white px-6 py-3 rounded hover:bg-gray-900 transition"
    >
      GitHub
    </a>
  </section>
</section>
